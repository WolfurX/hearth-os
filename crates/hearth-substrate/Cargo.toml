[package]
name = "hearth-substrate"
version = "0.1.0"
description = "The sovereignty substrate — snapshot-first transactions with one-gesture undo."
edition.workspace = true
license.workspace = true

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
